export class Node {
    x: number;
    y: number;
    size: number;
    label: string;
  
    constructor(x: number, y: number, size: number, label: string) {
      this.x = x;
      this.y = y;
      this.size = size;
      this.label = label;
    }
  }
  